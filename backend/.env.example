# Application
APP_NAME=
APP_VERSION=
ENVIRONMENT=
HOST=
PORT=

# Database
DATABASE_PATH=
VECTOR_DB_PATH=

# Logging
LOG_LEVEL=
LOGGING_CONFIG_PATH=

# Reporting
REPORT_DIRECTORY=

# Ollama / LLM
OLLAMA_HOST=
LLM_MODEL=
LLM_TEMPERATURE=
LLM_CONTEXT_WINDOW=
# Seconds to wait for one answer. A single question takes 85-250s on a
# 14B model, but answers slow markedly through a long batch run as host load
# accumulates: the integrated-assessment question took 207s in one run and
# 693s in the next. 1200 gives the tail of a 20-question batch headroom.
LLM_TIMEOUT=
